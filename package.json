{
  "name": "kanban-factory",
  "private": true,
  "version": "0.1.0",
  "description": "Kanban-style orchestrator for building apps with Claude agents",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c green,cyan \"npm:dev -w apps/server\" \"npm:dev -w apps/web\"",
    "build": "npm run build -w apps/server && npm run build -w apps/web",
    "smoke": "npm run smoke -w apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
